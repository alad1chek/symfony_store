{% extends 'base.html.twig' %}

{% block body %}
    <h1>Images list</h1>

    <table>
        <thead>
            <tr>
                <th>Url</th>
                <th>Id</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for image in images %}
            <tr>
                <td><a href="{{ path('crud-image_show', { 'id': image.id }) }}">{{ image.url }}</a></td>
                <td>{{ image.id }}</td>
                <td>
                    <ul>
                        <li>
                            <a href="{{ path('crud-image_show', { 'id': image.id }) }}">show</a>
                        </li>
                        <li>
                            <a href="{{ path('crud-image_edit', { 'id': image.id }) }}">edit</a>
                        </li>
                    </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('crud-image_new') }}">Create a new image</a>
        </li>
    </ul>
{% endblock %}
