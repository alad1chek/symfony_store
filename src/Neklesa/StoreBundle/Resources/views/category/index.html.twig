{% extends 'base.html.twig' %}

{% block body %}
    <h1>Categories list</h1>

    <table>
        <thead>
            <tr>
                <th>Name</th>
                <th>Parentid</th>
                <th>Id</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for category in categories %}
            <tr>
                <td><a href="{{ path('crud-categocy_show', { 'id': category.id }) }}">{{ category.name }}</a></td>
                <td>{{ category.parentId }}</td>
                <td>{{ category.id }}</td>
                <td>
                    <ul>
                        <li>
                            <a href="{{ path('crud-categocy_show', { 'id': category.id }) }}">show</a>
                        </li>
                        <li>
                            <a href="{{ path('crud-categocy_edit', { 'id': category.id }) }}">edit</a>
                        </li>
                    </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('crud-categocy_new') }}">Create a new category</a>
        </li>
    </ul>
{% endblock %}
